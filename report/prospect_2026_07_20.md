# prospect — ranked next steps for the NASCAR Cup model (2026-07-20)

*Read-only prospecting artifact. Proposes spikes for future, human-launched sessions;
changes nothing in the model. Pick by number in chat to get a model-grouped sprint schedule.*

## Preamble

**Objective function used for ranking (cited).** The repo's own north star: "a walk-forward
Plackett–Luce model of Cup Series finishing order, run as a **live forward test against the
closing-line market**" (`plan/schedule.yml:9-16`), whose central open question is "does ~0.41–0.45
correlation beat the closing line after the book's hold?" — "the only benchmark that can ever justify
'best in the world' — the closing line — remains unmeasured" (`report/NASCAR_AUDIT_REPORT.md:98,102`).
Two hard invariants constrain every idea: (1) "nothing changes the frozen model without a
pre-registered, walk-forward-gated A/B"; (2) "the perishable weekly market capture never pauses"
(`plan/schedule.yml:14-16`). **Payoff is therefore scored by how much an idea advances an
admissible, rigorous answer to the market-edge question — while protecting the frozen model and the
forward test's public integrity.**

**The one fact that dominates the ranking.** Almost the entire forward-looking backlog collapses
onto a single unmet condition: **the market benchmark has produced no verdict — N=0 admissible
priced picks.** Scoring only just began (race 5618, this session, rho=0.5458 —
`predictions/scores_log.csv`), and its 3 book prices are *inadmissible* because they were committed
23:26:57Z vs the 23:00:00Z scheduled green flag (`specs/market_benchmark_decision_rule.md:194-198`;
`report/OVERNIGHT_2026-07-20.md` §3). Downstream, the "≥8 scored races" gate blocks F1/F2/F8/F9/F18,
and the "market = EDGE" gate keeps the fully-frozen clean-air spec banked
(`specs/dnf_status_feature.md:5-7`; `specs/clean_air_causal_pace.md:29-43`). So the highest-payoff
work is whatever **fastest and most reliably grows the stock of admissible, scored, priced races** —
and whatever **protects the correctness of the engine that will eventually read that stock**.

**Verification reach achieved this run.** Read-only. All 10 gates were run green this session
(`report/OVERNIGHT_2026-07-20.md` §1); silver/gold/bronze were queried live via `data/nascar.duckdb`
(601 driver-race races across 3 series; bronze `failed`=0); the full plan (`plan/schedule.yml`, 2058
lines), all 7 `specs/`, `planning/aws_solutions.md`, `research/`, `report/NASCAR_AUDIT_REPORT.md`, and
every `src/*.py` were inventoried. Not done (out of scope for prospecting): running any A/B, rebuilding
silver/gold, or hitting any odds endpoint.

**Provenance stamp.** Generated by **`claude-opus-4-8[1m]`** (model ID observed in context). Per this
repo's `CLAUDE.md` tier map, Opus 4.8 **is** the top judgment tier — so this run is *at* recommended
tier, no below-tier caveat. Reasoning effort / thinking toggle are **not observable to me**; normally
I would ask, but this is an unattended overnight run launched as an Opus judgment-tier session, so
they are reported as-launched, not asserted. (Owner nuance: the auto-memory recommends Fable 5,
`claude-fable-5`, as a top-judgment option "where it adds value"; if you treat Fable as your true top,
read the moonshot reach as one notch below your ideal.)

**Grounding-audit pass: ran.** Before persisting, every declarative claim was swept for a source;
each is cited inline (`file:line`, a live query, a commit ref, or a named doc) or written as an
explicit assumption. Nothing load-bearing is unsourced.

---

## View 1 — the ranked grid

### TRANSFORMATIONAL

| # | Payoff | Effort | Idea | Flag | Model |
|---|--------|--------|------|------|-------|
| 1 | 3× the market-edge sample | E3 | Re-prove the model and run the forward test on **Xfinity + Trucks** (both already in silver) | [net-new] | Sonnet 5 (`claude-sonnet-5`) |
| 2 | Turns the project into a self-running, publicly-auditable market experiment | E4 | **Automate the full weekly loop** (predict→seal→push→capture→score→benchmark) as a hands-off local pipeline | [net-new] | Sonnet 5 |
| 3 | A data modality no public feed provides | E5 | **SMT broadcast-telemetry** feasibility + acquisition spike | [tracked] F17 ⚠ ToS | Fable 5 (`claude-fable-5`) / Opus 4.8 |

### HIGH

| # | Payoff | Effort | Idea | Flag | Model |
|---|--------|--------|------|------|-------|
| 4 | Stops the exact failure that just voided race 5618's prices | E1 | **Pre-green odds-admissibility self-check** run at price-recording time | [net-new] | Sonnet 5 |
| 5 | Protects the correctness of the engine that decides the whole project | E2 | **Fixture-test `market_benchmark.py`** (currently zero coverage) | [net-new] | Sonnet 5 |
| 6 | Unblocks reliable admissible odds capture (the bottleneck) | E2 | **L5: odds-source research + primary-book decision** | [tracked] L5 ⚠ ToS | Opus 4.8 (`claude-opus-4-8`) |

### MODERATE

| # | Payoff | Effort | Idea | Flag | Model |
|---|--------|--------|------|------|-------|
| 7 | README currency for the public repo | E2 | Document the **medallion stack** in README (it describes only the legacy pkl path) | [net-new] | Sonnet 5 |
| 8 | Catches a build-code regression a fresh rebuild would hide | E3 | **End-to-end build-code coverage** for silver_build/gold_build/predict_next runtime | [net-new] | Sonnet 5 |
| 9 | Fixes a documented calibration flaw in the market-facing product | E3 | **Probability recalibration** layer (gated A/B; underconfidence) | [net-new] | Fable 5 (spec) / Sonnet 5 (build) |

### MARGINAL

| # | Payoff | Effort | Idea | Flag | Model |
|---|--------|--------|------|------|-------|
| 10 | Removes a 4× copy-paste HTTP helper | E1 | Centralize the duplicated `fetch()` helper | [net-new] | Sonnet 5 |
| 11 | Removes two one-off dead scripts from src/ | E1 | Retire/relocate `bronze_probe_2017*.py` | [net-new] | Haiku 4.5 (`claude-haiku-4-5`) |

**Bottom line.** 11 items — 3 TRANSFORMATIONAL (quota met), 3 HIGH, 3 MODERATE, 2 MARGINAL. The
top of the list is dominated by one theme: **grow the admissible-priced-race sample faster and more
reliably** (items 1, 2, 4, 6) and **harden the engine that will read it** (item 5). Everything
model-feature-shaped (the F-series A/Bs) is deliberately absent — all of it is gated on ≥8 scored
races or market=EDGE, neither reachable yet, so proposing it now would be padding, not signal.

---

## View 2 — detail blocks

### 1 — Re-prove the model + forward test on Xfinity & Trucks — TRANSFORMATIONAL · E3 · [net-new]

Extend the Cup-only model and forward test to the two other national series, which the foundation
already holds end-to-end.

**How / why.** Silver already carries all three series: a live query this session returned **601
distinct (series_id, race_id) in `silver.driver_race`** across series 1/2/3, and `silver.results`
has 932 (`report/OVERNIGHT_2026-07-20.md` §T4 checks; `data/nascar.duckdb`). But the model trains
Cup-only — `predict_next.py` and `walkforward.py` read `races_parsed.pkl`, which is `race_type_id=1`
/ `series_1` only (`src/update_data.py:23`), and `MY_TYPE` is a Cup-track typology
(`src/walkforward.py:23-35`). Xfinity and Trucks run a heavily overlapping schedule, so re-proving
there **roughly triples the number of races per calendar week feeding the market benchmark** — which
is the binding constraint on ever reaching the benchmark's K≥20 / N≥50 floors
(`specs/market_benchmark_decision_rule.md:168-171,77`). This is the single largest lever on
*time-to-answer* for the central question that does not depend on any external party.

**Backlog vs net-new.** Net-new. The plan's F-series is entirely Cup single-series; no item proposes
multi-series. Not tracked anywhere.

**Ethos check.** Compatible: it does not change the frozen Cup model (a separate per-series fit),
and it must earn its own walk-forward proof exactly as Cup did (`plan/schedule.yml:14`,
"RE-PROVE on the new foundation rather than re-choose").

**Spike setup — Part A (model + settings).** Sonnet 5 (`claude-sonnet-5`), thinking on, effort xhigh.
Scope: a **research+build** session that fits and walk-forward-evaluates the existing engine on
Xfinity/Trucks and reports whether the ρ/typology transfer holds — **not** a session that changes the
Cup model or adopts anything into production without its own gate.

**Spike setup — Part B (kickoff prompt):**
```
Read HANDOFF.md and specs/medallion_architecture.md, then resume. This is a research+build
spike: extend the walk-forward PL model and forward test to the Xfinity (series_id=2) and
Truck (series_id=3) series, which already exist in silver.

Run first: `cd src && python run_gates.sh` (all 10 must be green). Then query
data/nascar.duckdb read-only to confirm per-series race counts in silver.driver_race /
silver.results and inspect the track set per series.

Context (cite-checkable): silver holds all 3 series (601 distinct (series,race) in
silver.driver_race; DATA_DICTIONARY.md §9). The Cup model is series_1-only: update_data.py:23
filters race_type_id==1/series_1; walkforward.py:23-35 MY_TYPE is a Cup-track typology. The
market benchmark's K>=20/N>=50 floors (market_benchmark_decision_rule.md:168-171,77) are the
binding constraint on time-to-answer; 3x the races/week directly attacks it.

Do: build a per-series parsed dataset from silver (NOT a new download), fit the frozen config's
engine per series, run the walk-forward backtest per series, and report the ρ trio + a per-series
typology check. Pre-register a short spec first (specs/) if any of this would later feed a
production forward test.

Scope boundary: DO NOT modify walkforward.py, predict_next.py, MY_TYPE, or any frozen config;
DO NOT adopt a multi-series model into the live forward test without its own pre-registered,
walk-forward-gated proof (that adoption is a separate future session). Superspeedway stand-down
doctrine applies per series. Exit criteria: a written report (report/) with per-series ρ and a
go/no-go recommendation on standing up a per-series forward test; frozen Cup model untouched;
all 10 gates still green. Model: Sonnet 5, thinking on, effort xhigh.
```
> On execution, first verify the running model ID matches `claude-sonnet-5` and ASK whether
> thinking is on and effort is xhigh before substantive work.

---

### 2 — Automate the full weekly loop into a self-running local pipeline — TRANSFORMATIONAL · E4 · [net-new / synthesizes L3+L4]

Wire the existing pieces (predict → seal → push → capture odds → score → benchmark) into a hands-off
scheduled local pipeline, so the market-edge question answers itself over a season with full public
auditability.

**How / why.** Every component exists: `predict_next.py` (seals + refuses post-hoc,
`README.md:57-61`), the GitHub remote + push (E2 done, `plan/schedule.yml:958`), `update_data.py`'s
fetch loop, `score_race.py` (D2), `market_benchmark.py` (D2). The plan already specs the local
scheduling primitives — **L3 (launchd weekly pipeline)** and **L4 (pmset live poller)** — as
plan-only, with L3 "gated on GitHub remote existing," now met (`plan/schedule.yml:1907-1939`;
`planning/aws_solutions.md:41`). The transformational step is not any one job but *closing the loop*:
a scheduled rig that produces a sealed, pushed prediction before every green flag, captures closing
odds pre-flag (item 4 guards admissibility), scores after results post, and reruns the benchmark —
with zero manual steps. That converts "a model plus a manually-run forward test" into "a self-running,
publicly-timestamped market-edge experiment," which is what makes any eventual EDGE/NO-EDGE verdict
credible to an outside reader (`report/NASCAR_AUDIT_REPORT.md:98-102`).

**Backlog vs net-new.** Partly tracked (L3, L4 exist as plan-only sessions) but the *integration into
one closed loop* is net-new; no single plan item owns it.

**Ethos check.** Compatible and protective: automation *strengthens* invariant (2) ("capture never
pauses") — this session's own finding is that a *manual* capture missed the pre-flag window and voided
5618's prices. ⚠ One caution: must NOT use GitHub Actions (the repo explicitly rejects it as a quota
trap — `plan/schedule.yml:2011`); local launchd/pmset only.

**Spike setup — Part A.** Sonnet 5 (`claude-sonnet-5`), thinking on, effort xhigh. Scope: a **build**
session that composes existing scripts into scheduled local jobs with health-checks and idempotency;
**not** a session that changes any model, spec, or scoring logic.

**Spike setup — Part B (kickoff prompt):**
```
Read HANDOFF.md, planning/aws_solutions.md (L1-L5), and plan/schedule.yml L3/L4, then resume.
Build a hands-off LOCAL weekly pipeline that closes the forward-test loop.

Run first: src/run_gates.sh (all 10 green).

Context (cite-checkable): all components exist — predict_next.py (seals+refuses post-hoc,
README.md:57-61), remote+push (E2, schedule.yml:958), update_data.py fetch loop, score_race.py
+ market_benchmark.py (D2). L3 (launchd weekly) and L4 (pmset poller) are plan-only and L3's
"remote exists" gate is met (schedule.yml:1907-1939). GitHub Actions is explicitly forbidden as
a quota trap (schedule.yml:2011) — use launchd/pmset only.

Do: implement a scheduled local orchestration that (a) after qualifying: update_data ->
predict_next -> commit+push the sealed prediction BEFORE green flag; (b) records closing odds
pre-flag and runs item-4's admissibility self-check; (c) after results: update_data ->
bronze_fetch --update -> --sync-legacy-cache -> score_race -> market_benchmark; (d) logs health
and is idempotent/re-runnable. Everything follows the frozen specs verbatim.

Scope boundary: DO NOT change any model, frozen spec, or scoring logic; DO NOT use GitHub
Actions; DO NOT auto-perform the D2 cutover (re-pointing predict_next.py) — that stays
owner-gated (specs/medallion_architecture.md:1213-1217). Exit criteria: a documented, tested,
idempotent local pipeline that runs one full cycle end-to-end in a dry run; all 10 gates green.
Model: Sonnet 5, thinking on, effort xhigh.
```
> On execution, verify model ID is `claude-sonnet-5` and ASK about thinking/effort first.

---

### 3 — SMT broadcast-telemetry feasibility + acquisition spike — TRANSFORMATIONAL · E5 · [tracked F17] ⚠ ToS/feasibility

Research whether NASCAR's SMT broadcast telemetry (real per-car sensor data) is acquirable and
legal to use — a data modality no public `cf.nascar.com` feed provides.

**How / why.** This is a *tracked, owner-elevated* moonshot: plan session **F17** (`plan/schedule.yml:1648`),
"owner-directed 2026-07-19 ('too important — heavy, heavy research')," runnable now, "proposes only"
(`:1692-1703`), recommended model **Fable 5 · xhigh**. If a telemetry stream were obtainable, it would
replace lap-time *proxies* for pace with actual car speed/throttle/brake — directly feeding the
clean-air-pace question the audit calls a proven dead end via crude proxies
(`report/NASCAR_AUDIT_REPORT.md:86`). **Premise honesty:** whether the feed is acquirable at all is
itself unknown — this spike IS the feasibility research; it does not promise data, it grades whether
the modality is real and reachable. That is a legitimate transformational premise (§ big-end bar =
premise, not outcome).

**Backlog vs net-new.** Tracked: F17, `pending`, owner-prioritized.

**Ethos check.** ⚠ ToS/legal-gated exactly like the odds route: resolve posture first, probe only
public unauthenticated surfaces, hit no gated endpoint. Sits alongside the open A6 ToS review
(`plan/schedule.yml:216`).

**Spike setup — Part A.** Fable 5 (`claude-fable-5`) or Opus 4.8 (`claude-opus-4-8`), thinking on,
effort xhigh — top judgment tier, because the deliverable is a feasibility/methodology/ToS judgment
where being wrong (e.g. asserting a source is usable when it isn't) is costly. Scope: **research/propose
only**; no acquisition, no scraping, no model change.

**Spike setup — Part B (kickoff prompt):**
```
Read HANDOFF.md, plan/schedule.yml session F17, and specs/README.md, then resume. This is a
RESEARCH-ONLY feasibility spike (F17): assess SMT broadcast-telemetry as a data modality — does
it exist in an acquirable, legally-usable form for this project? Propose only; acquire nothing.

Context (cite-checkable): F17 is owner-prioritized ("too important — heavy, heavy research",
schedule.yml:1648-1703, "proposes only"). Current pace features are lap-time proxies the audit
found weak for clean-air pace (NASCAR_AUDIT_REPORT.md:86). ToS posture is unresolved and gates
this exactly like the odds route (A6, schedule.yml:216).

Do: map what SMT telemetry is, who controls it, what (if any) public unauthenticated surface
exists, the licensing/ToS posture, and a concrete acquisition-feasibility verdict with an
evidence ledger. If a premise can only be confirmed by hitting a gated endpoint, STOP and say so.

Scope boundary: DO NOT scrape, acquire, or hit any gated/authenticated endpoint; DO NOT change
any model or spec; probe only public unauthenticated surfaces. Exit criteria: a written
feasibility report (research/) with a go/no-go and, if go, a proposed next spike. Model: Fable 5
(or Opus 4.8), thinking on, effort xhigh.
```
> On execution, verify model ID matches the header and ASK about thinking/effort first.

---

### 4 — Pre-green odds-admissibility self-check — HIGH · E1 · [net-new]

A small tool that, at odds-recording time, verifies the recorded prices *will* count as admissible
(committed before the race's scheduled start) — so the exact failure that voided race 5618's prices
never recurs.

**How / why.** Race 5618's 3 prices were ruled inadmissible because their first commit (23:26:57Z)
postdated the scheduled green flag (23:00:00Z) — the admissibility rule keys on git-commit-vs-start
(`specs/market_benchmark_decision_rule.md:194-198`; `report/OVERNIGHT_2026-07-20.md` §3). The logic
to compute this already exists: `score_race.py:317` `_green_flag_utc(year, race_id)` and its
git-first-commit-vs-green-flag comparison (`post_race_price_note`). This spike wraps that same logic
into a **pre-race** check the weekly protocol runs right after recording prices: "these entries, if
committed now, are admissible — commit and push before {start_time_utc}." It is nearly all reuse and
prevents silent loss of the *only* data the north-star question depends on.

**Backlog vs net-new.** Net-new. The failure mode is documented (`report/OVERNIGHT_2026-07-20.md` §5b
fork 3 region and §3) but no tool guards it.

**Ethos check.** Directly serves invariant (2) — it makes the perishable capture reliable.

**Spike setup — Part A.** Sonnet 5 (`claude-sonnet-5`), thinking on, effort high. Scope: **build** a
read-only pre-race check; no scoring/model/spec change.

**Spike setup — Part B (kickoff prompt):**
```
Read HANDOFF.md and specs/market_benchmark_decision_rule.md (admissibility amendment), then
resume. Build a small pre-race odds-admissibility self-check.

Run first: src/run_gates.sh (all 10 green).

Context (cite-checkable): admissibility keys on git-first-commit-vs-scheduled-start
(market_benchmark_decision_rule.md:194-198); race 5618's prices were voided for being committed
27 min late (OVERNIGHT_2026-07-20.md §3). The comparison logic already exists in
score_race.py:317 (_green_flag_utc) and post_race_price_note.

Do: add a read-only command that, given a race_id + its prediction JSON's current book_prices,
reports whether committing now would be admissible (before start_time_utc) and prints the
remaining time-to-flag. Reuse score_race.py's helpers; do not duplicate the green-flag lookup.
Add a fixture test in the test_score_race style (admissible vs inadmissible cases) and wire it
into run_gates.sh if it becomes a standing check.

Scope boundary: read-only w.r.t. predictions and results; DO NOT change score_race.py's scoring
math, any frozen spec, or the model. Exit criteria: the check correctly flags a synthetic
inadmissible case and passes a synthetic admissible one; all gates green. Model: Sonnet 5,
thinking on, effort high.
```
> On execution, verify model ID `claude-sonnet-5`, ASK about thinking/effort.

---

### 5 — Fixture-test the market-benchmark decision engine — HIGH · E2 · [net-new]

`market_benchmark.py` — the module that will produce the project's single most important verdict
(EDGE / NO-EDGE / UNDERPOWERED) — has **zero test coverage**. Give it the same fixture treatment
`score_race.py` already has.

**How / why.** The scan found `market_benchmark.py` (~360 lines, 12 functions + `main()`) is
untested — "no coverage at all"; `compute_state`, `evaluate_final`, `final_look_triggered`,
`reconstruct_and_decide`, `by_config_split` are all unexercised (code-health scan §2). Yet its spec
pins **precise, fixture-able thresholds**: EDGE at N≥50 & bootstrap `p≤0.001` (plus a t-test
`p_t≤0.001`), NO-EDGE at N≥100 & 95% upper bound <0, final-look precedence, the K≥20
UNDERPOWERED floor, and the exact admissibility rule
(`specs/market_benchmark_decision_rule.md:75-79,168-182,192-206`). A bug here, discovered only when
the benchmark first fires on real data, would **corrupt the one answer the project exists to
produce** — a high cost-of-being-wrong that argues for coverage *before* N grows. The proven pattern
already exists: `test_score_race.py`'s 10 stdlib fixtures (`specs/scoring_methodology.md:276-318`).

**Backlog vs net-new.** Net-new. No plan item covers testing the benchmark engine.

**Ethos check.** Pure protection of the north-star gate; touches no model.

**Spike setup — Part A.** Sonnet 5 (`claude-sonnet-5`), thinking on, effort high. Scope: **build**
tests only; do not modify `market_benchmark.py`'s logic unless a test surfaces a provable defect
(and then only with the defect documented).

**Spike setup — Part B (kickoff prompt):**
```
Read specs/market_benchmark_decision_rule.md (all amendments) and src/market_benchmark.py, then
resume. Write a fixture test suite for the market-benchmark decision engine (currently untested).

Run first: src/run_gates.sh (all 10 green).

Context (cite-checkable): market_benchmark.py has zero coverage (12 functions incl.
compute_state/evaluate_final/final_look_triggered/reconstruct_and_decide untested). The spec pins
exact thresholds: EDGE N>=50 & bootstrap p<=0.001 & t-test p_t<=0.001; NO-EDGE N>=100 & 95% UB<0;
final-look precedence; K>=20 UNDERPOWERED floor; admissibility = first-commit-before-start
(market_benchmark_decision_rule.md:75-79,168-182,192-206). test_score_race.py:F1-F10 is the
proven stdlib-fixture pattern to mirror.

Do: build src/test_market_benchmark.py with synthetic scores_log + book_prices fixtures that
exercise each decision boundary (EDGE early-stop, NO-EDGE futility, UNDERPOWERED, final-look,
admissibility include/exclude, seed determinism). Use a fixed seed; assert byte-stable verdicts.
Wire it into run_gates.sh as gate 11 once green.

Scope boundary: DO NOT change market_benchmark.py's logic except to fix a defect the tests prove
(document it if so); DO NOT change the frozen spec or any model. Exit criteria: all fixtures pass
AND are verified to go red on a deliberately perturbed threshold; run_gates.sh green with the new
gate. Model: Sonnet 5, thinking on, effort high.
```
> On execution, verify model ID `claude-sonnet-5`, ASK about thinking/effort.

---

### 6 — L5: odds-source research + primary-book decision — HIGH · E2 · [tracked L5] ⚠ ToS

Decide the primary sportsbook and the capture route for H2H matchup prices — the prerequisite that
unblocks reliable local odds capture (L2).

**How / why.** Tracked plan session **L5** (`plan/schedule.yml:1940`, Opus 4.8 · xhigh): a research
spike whose finding is already partly recorded — "The Odds API does NOT cover NASCAR"; the DraftKings
unofficial JSON is free but ToS-gated; **the primary-book choice is still OPEN** (owner did not
designate one) (`plan/schedule.yml:1953-1963`). This matters because admissibility now also requires
*primary-book binding* — only primary-book entries count
(`specs/market_benchmark_decision_rule.md:262-276`) — and **L2 (local closing-odds capture) is gated
directly on L5** (`plan/schedule.yml:1890,1905`). Until the book is chosen, every recorded price risks
the same fate as 5618's. This is the bottleneck-opener for the entire market-edge program.

**Backlog vs net-new.** Tracked: L5 (pending/ready), unblocks L2.

**Ethos check.** ⚠ Internally ToS-gated: the plan mandates "STOP after step 2, hit no endpoint until
posture resolved" (`plan/schedule.yml:1963`); coordinates with the open A6 ToS review.

**Spike setup — Part A.** Opus 4.8 (`claude-opus-4-8`), thinking on, effort xhigh — top judgment tier
per the plan's own assignment; the deliverable is a source/ToS/methodology judgment.

**Spike setup — Part B (kickoff prompt):**
```
Read HANDOFF.md, plan/schedule.yml session L5, specs/market_benchmark_decision_rule.md
(primary-book binding), and specs/README.md, then resume. This is plan session L5: research odds
sources and RECOMMEND a primary sportsbook + capture route. Propose only.

Context (cite-checkable): L5 is Opus·xhigh, internally ToS-gated — STOP after step 2, hit no
endpoint until posture resolved (schedule.yml:1953-1963). The Odds API does not cover NASCAR;
DK unofficial JSON is free but ToS-gated; primary-book choice is OPEN. Admissibility requires
primary-book binding (market_benchmark_decision_rule.md:262-276); L2 capture is gated on L5
(schedule.yml:1890,1905).

Do: survey what H2H-matchup NASCAR prices are actually obtainable, the ToS posture of each route,
and recommend a primary book + capture method + admissibility-safe recording workflow. Produce an
evidence ledger.

Scope boundary: STOP after the research step; DO NOT hit any gated/authenticated odds endpoint;
DO NOT implement capture (that is L2, a separate session). Exit criteria: a written
recommendation with a primary-book decision put to the owner and a route that keeps prices
admissible. Model: Opus 4.8, thinking on, effort xhigh.
```
> On execution, verify model ID `claude-opus-4-8`, ASK about thinking/effort.

---

### 7 — Document the medallion stack in README — MODERATE · E2 · [net-new]

The public README's "Pipeline" and "Repository map" describe only the legacy `download → parse →
predict` pkl path and omit the current bronze/silver/gold medallion stack.

**How / why.** The scan confirmed README (`README.md:39-45,78-90`) documents only the legacy path;
the medallion stack (`bronze_fetch.py`, `warehouse.py`, `silver_build.py`, `gold_build.py`, the gates)
is documented only in `GATES.md`, `specs/medallion_architecture.md`, and `DATA_DICTIONARY.md`
(code-health scan §5). A reader of the public repo sees a materially smaller/older system than what
`src/` actually is. **Judgment note (why only MODERATE):** README is deliberately a high-level public
overview; the omission may be intentional editorial scoping, not an accident — so this is a
presentation call for the owner, not clearly-correct drift. It is *not* a broken reference (every
path README cites exists — scan §5).

**Backlog vs net-new.** Net-new; related to the OVERNIGHT-report's queued "README first-rho" question
(`report/OVERNIGHT_2026-07-20.md` §5b fork 4).

**Ethos check.** Compatible; public-facing accuracy.

**Spike setup — Part A.** Sonnet 5 (`claude-sonnet-5`), thinking on, effort high. Scope: **docs-only**;
confirm the editorial intent with the owner first, since it changes the public face of the repo.

**Spike setup — Part B (kickoff prompt):**
```
Read README.md, GATES.md, DATA_DICTIONARY.md §8-11, and specs/medallion_architecture.md, then
resume. Propose (and, if the owner confirms the editorial intent, make) a README update that
documents the medallion stack alongside the legacy pipeline.

Context (cite-checkable): README:39-45,78-90 describes only download->parse->predict; the
medallion stack (bronze_fetch/warehouse/silver_build/gold_build + 10 gates) is documented only in
GATES.md/specs/DATA_DICTIONARY. This is presentation scope, not a broken reference.

Do: draft a short README section covering the medallion foundation + src/run_gates.sh, keeping
the public tone. Confirm with the owner whether the public README should surface it before
committing (it changes the repo's public face).

Scope boundary: docs only; DO NOT change code, gates, or specs; DO NOT surface the forward-test
rho on README without an explicit owner decision (separate queued question). Exit criteria: a
README that matches the actual src/ or a recorded owner decision to keep it high-level. Model:
Sonnet 5, thinking on, effort high.
```
> On execution, verify model ID `claude-sonnet-5`, ASK about thinking/effort.

---

### 8 — End-to-end build-code coverage — MODERATE · E3 · [net-new]

The two heaviest build scripts and the live entrypoint are only indirectly covered: the gates
validate the *artifacts*, not the code that builds them.

**How / why.** Scan §2: `gate_silver.py` does not import `silver_build.py` (only references it in a
comment, `gate_silver.py:249`) and requires the build to have been run separately first
(`gate_silver.py:5-6`); `gate_gold.py` re-implements gold math independently rather than importing
`gold_build.py` ("Same math as gold_build.py's SQL, independently written," `gate_gold.py:180`);
`predict_next.py`'s runtime (grid read, refit, JSON/MD/CSV write, refuse-if-results-exist) is only
*statically* parsed, never executed (`test_frozen_config.py:18`). So a regression in a build script
that still happened to reproduce today's artifact — or, worse, one that only manifests on a *fresh*
rebuild from bronze — would pass every gate. The independent re-implementation in `gate_gold.py` is a
genuine strength (catches divergence) but means the build code itself is never the thing under test.

**Backlog vs net-new.** Net-new. (The audit-step modules step2/3/4/6 are documented "frozen audit
artifacts," `specs/medallion_architecture.md:1005`, and are legitimately out of scope.)

**Ethos check.** Compatible; strengthens the reproducibility guarantee the medallion rebuild exists to
provide.

**Spike setup — Part A.** Sonnet 5 (`claude-sonnet-5`), thinking on, effort xhigh. Scope: **build**
a from-bronze rebuild-and-verify path or a smoke test that executes the build code; touch no frozen
logic.

**Spike setup — Part B (kickoff prompt):**
```
Read specs/medallion_architecture.md §3-6 and GATES.md, then resume. Close the build-code coverage
gap: today's gates validate artifacts, not the code that builds them.

Run first: src/run_gates.sh (all 10 green).

Context (cite-checkable): gate_silver.py does not import silver_build.py (comment only, :249;
requires a prior separate build, :5-6); gate_gold.py independently re-implements gold math rather
than importing gold_build.py (:180); predict_next.py runtime is only ast-parsed, never executed
(test_frozen_config.py:18). A build-script regression that reproduced today's artifact — or only
broke on a fresh rebuild — would pass every gate.

Do: add a gated end-to-end check that actually EXECUTES the build path from bronze (or a scoped
subset) and asserts the rebuilt silver/gold matches the committed anchor/expected values; and/or
a predict_next smoke test in a temp dir with a stubbed grid. Reuse existing anchors; keep it
deterministic. Wire into run_gates.sh.

Scope boundary: DO NOT change frozen build logic, the model, or the anchor; a rebuild must be
non-destructive (temp/throwaway warehouse, not data/nascar.duckdb). Exit criteria: a green,
deterministic gate that executes build code end-to-end; all gates green. Model: Sonnet 5,
thinking on, effort xhigh.
```
> On execution, verify model ID `claude-sonnet-5`, ASK about thinking/effort.

---

### 9 — Probability recalibration layer (gated A/B) — MODERATE · E3 · [net-new]

Address the audit's documented underconfidence with a calibration layer on the win/H2H probabilities
— under a pre-registered, walk-forward-gated A/B, exactly like any model change.

**How / why.** The audit measures systematic underconfidence: "regularization makes the model
*underconfident* (when it says 64%, reality is 74%)" (`report/NASCAR_AUDIT_REPORT.md:90`; also
`README.md:106-108`). A monotone recalibration (isotonic/Platt) on `p_win`/`p_top5`/`p_top10` and the
`h2h_prob` matrix would sharpen the public product's probabilities. **Honest scoping (why MODERATE,
not HIGH):** a monotone recalibration does **not** change the finishing-*order* ρ and, because it
preserves which side of 0.5 each H2H sits on, does **not** change the current *pick-based* market
benchmark's picks (`specs/market_benchmark_decision_rule.md:59-60` grades picks, not stakes). Its
payoff lands only on (a) calibration/Brier quality of the published probabilities and (b) any *future*
confidence- or stake-weighted betting layer, where it would become HIGH. It must still clear its own
pre-registered A/B (invariant 1); calibration/Brier, not ρ, is its kill-criterion metric.

**Backlog vs net-new.** Net-new. Not among the F-series (which target the ordering model / features).

**Ethos check.** ⚠ It is a change to a model *output*, so it is bound by invariant (1): no adoption
without a pre-registered walk-forward-gated A/B. Frame the spec accordingly.

**Spike setup — Part A.** Fable 5 (`claude-fable-5`) for the spec (top judgment tier — pre-registration
design), Sonnet 5 (`claude-sonnet-5`) for the build; thinking on, effort xhigh. Scope: **pre-register
then A/B**; adopt only if the frozen kill/keep rule fires.

**Spike setup — Part B (kickoff prompt):**
```
Read report/NASCAR_AUDIT_REPORT.md (underconfidence, :90), specs/README.md (pre-registration
rules), and specs/scoring_methodology.md, then resume. Pre-register, then run, a probability-
recalibration A/B.

Run first: src/run_gates.sh (all 10 green).

Context (cite-checkable): audit measures underconfidence (says 64%, reality 74%,
NASCAR_AUDIT_REPORT.md:90; README.md:106-108). Recalibration is monotone: it does NOT change ρ or
the pick-based benchmark's picks (market_benchmark_decision_rule.md:59-60), so its metric is
calibration/Brier, and its value is highest for a future stake-weighted layer. It is a model-output
change and needs a gated A/B (schedule.yml:14).

Do: write a frozen spec (specs/) defining the calibration method (isotonic/Platt), the walk-forward
protocol, and a kill/keep rule on Brier/calibration (NOT ρ); then run it as a separate step script
(src/), leaving predict_next.py/walkforward.py untouched unless the rule fires.

Scope boundary: DO NOT modify the frozen ordering model or config; adopt nothing without the
pre-registered rule firing; ρ is not the decision metric. Exit criteria: a committed spec + an
A/B result (adopt or documented null); frozen model untouched unless the rule fired; all gates
green. Model: Fable 5 (spec) / Sonnet 5 (build), thinking on, effort xhigh.
```
> On execution, verify model ID matches the header and ASK about thinking/effort.

---

### 10 — Centralize the duplicated `fetch()` helper — MARGINAL · E1 · [net-new]

**How / why.** A near-identical urllib `fetch()` is copy-pasted 4×: `download.py:8` (3-retry variant),
and byte-for-byte in `predict_next.py:19`, `update_data.py:8`, `gold_predict_dryrun.py:29` (only the
timeout differs, 30 vs 60) — code-health scan §4. Consolidating into one small shared helper removes
the copy-paste without behavior change. **Correctness note:** leave the *walkforward-mirror*
duplication alone — those re-implementations (predict_next / gold_predict_dryrun / gate_gold mirroring
`walkforward.run`) are *documented-intentional independence copies* (scan §4), and coupling them would
weaken the gates' cross-check; only the trivial HTTP helper is safe to share.

**Spike setup — Part A.** Sonnet 5 (`claude-sonnet-5`), thinking on, effort high. Scope: mechanical
refactor, behavior-preserving.

**Spike setup — Part B (kickoff prompt):**
```
Read HANDOFF.md, then resume. Behavior-preserving refactor: centralize the 4x-duplicated urllib
fetch() helper (download.py:8, predict_next.py:19, update_data.py:8, gold_predict_dryrun.py:29 —
only timeout differs) into one shared helper with a timeout parameter.

Run first: src/run_gates.sh (all 10 green).

Do: extract a single fetch(url, timeout) helper, import it in all four call sites, preserve exact
behavior (headers, timeouts). Confirm all 10 gates still green.

Scope boundary: DO NOT touch the walkforward-mirror training-loop duplication (predict_next /
gold_predict_dryrun / gate_gold) — those are intentional independence copies (scan §4). No model
or spec change. Exit criteria: one shared fetch helper, all call sites updated, all gates green,
identical behavior. Model: Sonnet 5, thinking on, effort high.
```
> On execution, verify model ID `claude-sonnet-5`, ASK about thinking/effort.

---

### 11 — Retire/relocate the one-off 2017-probe scripts — MARGINAL · E1 · [net-new]

**How / why.** `src/bronze_probe_2017.py` and `src/bronze_probe_2017_remaining.py` are explicitly
"one-off, not part of `--full`/`--update`" (`DATA_DICTIONARY.md:326`), imported nowhere in code, and
referenced only in historical plan/handoff prose (code-health scan §4). The 2017-recovery work they
served is done (B4, `a588563`). Moving them to an `archive/` or `research/` location (or removing them
with a note) de-clutters `src/` so the flat directory reads as live pipeline only. **Correctness
note:** confirm no gate or script imports them before moving (scan says none do — verify at spike
time).

**Spike setup — Part A.** Haiku 4.5 (`claude-haiku-4-5`), thinking on, effort high. Scope: mechanical
tidy; verify-no-references first.

**Spike setup — Part B (kickoff prompt):**
```
Read DATA_DICTIONARY.md §8 and HANDOFF.md, then resume. Tidy: relocate or remove the one-off
2017-probe scripts (src/bronze_probe_2017.py, src/bronze_probe_2017_remaining.py) now that B4's
2017 recovery is done (a588563).

Run first: `grep -rn bronze_probe src/` and confirm NOTHING imports them (DATA_DICTIONARY.md:326
says one-off, not in --full/--update). Also run src/run_gates.sh (all 10 green).

Do: if no code references them, move them under an archive/ or research/ path (or remove with a
one-line note in the commit), preserving git history. Re-run all 10 gates.

Scope boundary: DO NOT remove anything still imported; DO NOT touch any pipeline/gate script.
Exit criteria: src/ contains only live pipeline/gate/test files or the probes are clearly
archived; all gates green. Model: Haiku 4.5, thinking on, effort high.
```
> On execution, verify model ID `claude-haiku-4-5`, ASK about thinking/effort.

---

## How to pick

Reply with the numbers you want (e.g. `1, 4-6`, `all of HIGH`, `1 5 6; delay 2; skip the rest`).
Unnamed items default to skip. On your pick I'll produce a model-grouped **sprint schedule**
(`report/prospect_schedule_<date>.md`) — phases grouped by model+settings so you flip the model once
per phase — and stop there; spikes are launched by you in fresh sessions. Note the repo's
consolidation-cadence rule (roughly every 4th session is consolidation-only) will be woven into that
schedule.
